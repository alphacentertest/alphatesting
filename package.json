{
  "name": "alphatest",
  "version": "1.0.0",
  "dependencies": {
    "connect-redis": "^7.1.1",
    "cookie-parser": "^1.4.6",
    "exceljs": "^4.4.0",
    "express": "^4.19.2",
    "express-session": "^1.18.0",
    "redis": "^4.7.0"
  },
  "scripts": {
    "clean": "rimraf node_modules package-lock.json && npm cache clean --force && npm install"
  },
  "devDependencies": {
    "rimraf": "^6.0.1"
  }
}
